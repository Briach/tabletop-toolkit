import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.fabianbrecht.tabletoptoolkit',
  appName: 'Tabletop Toolkit',
  webDir: 'dist',
  bundledWebRuntime: false
};

export default config;
